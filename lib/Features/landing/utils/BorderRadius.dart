import 'package:flutter/material.dart';

final cardBorderRadius = BorderRadius.circular(5);
final tabBorderRadius = BorderRadius.circular(50);
const oneSideBorderRadius = BorderRadius.horizontal(left: Radius.circular(30));
